<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_gray">

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/main_navi"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        app:itemBackground="@drawable/main_navi_back_selector"
        app:itemIconSize="30dp"
        app:itemIconTint="@drawable/main_color_selector"
        app:itemTextColor="@drawable/main_color_selector"
        app:itemActiveIndicatorStyle="@color/transparent"
        app:labelVisibilityMode="labeled"
        app:itemTextAppearanceActive="@style/pretendard_medium"
        app:itemTextAppearanceInactive="@style/pretendard_medium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="70dp"
        android:background="@color/dark_gray"
        app:menu="@menu/navi_menu"
        app:elevation="0dp"/>

    <FrameLayout
        android:id="@+id/main_frm"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="@color/white"
        app:layout_constraintTop_toBottomOf="@id/main_navi"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintVertical_bias="0.0"
        tools:layout_editor_absoluteX="0dp" />

</androidx.constraintlayout.widget.ConstraintLayout>